<div>
    <div class="paypal-method-name">
        ${dw.order.PaymentMgr.getPaymentMethod(payment.paymentMethod).getName()}
    </div>
    <div class="paypal-amount">
        ${dw.util.StringUtils.formatMoney(new dw.value.Money(payment.amount, pdict.braintree.currency))}
    </div>
</div>
